'use client';
import Link from 'next/link';import { usePathname } from 'next/navigation';import { navItems } from '@/data/mock';
export function MobileBottomNav(){const p=usePathname();const items=navItems.filter(i=>['/dashboard','/contracts','/documents','/notifications','/profile'].includes(i.href));return <nav className="fixed inset-x-3 bottom-3 z-20 grid grid-cols-5 rounded-[1.6rem] border border-white/20 bg-brand-900/95 p-1.5 text-white shadow-soft backdrop-blur lg:hidden">{items.map(({href,label,icon:Icon})=>{const active=p===href||p.startsWith(`${href}/`);return <Link key={href} href={href} className={`flex flex-col items-center gap-1 rounded-2xl py-2 text-[10px] font-semibold ${active?'bg-cta text-brand-900':'text-white/65'}`}><Icon size={18}/>{label}</Link>})}</nav>}
