export function Stepper({ step }: { step: number }) { const steps=['Договор','Способ оплаты','Подтверждение']; return <div className="grid grid-cols-3 gap-2">{steps.map((s,i)=><div key={s} className={`rounded-2xl px-3 py-2 text-center text-xs font-extrabold ${i+1<=step?'bg-brand-900 text-white':'bg-white text-slate-400'}`}>{s}</div>)}</div>; }
