export function Stepper({ step }: { step: number }) { const steps=['Договор','Оплата','Готово']; return <div className="flex gap-2">{steps.map((s,i)=><div key={s} className={`flex-1 rounded-full px-3 py-2 text-center text-xs font-bold ${i+1<=step?'bg-cta text-brand-900':'bg-white text-slate-400'}`}>{s}</div>)}</div>; }
