import { ShieldCheck } from 'lucide-react';
import { Button } from './Button';import { Card } from './Card';import { ProgressBar } from './ProgressBar';import { StatusBadge } from './StatusBadge';
import type { Contract } from '@/data/mock';
export function ContractCard({ contract }: { contract: Contract }) { return <Card className="space-y-5"><div className="flex items-start justify-between gap-3"><div className="flex gap-3"><div className="rounded-2xl bg-lavender p-3 text-brand-900"><ShieldCheck /></div><div><h3 className="text-xl font-black text-brand-900">{contract.title}</h3><p className="text-sm text-slate-500">{contract.type}</p></div></div><StatusBadge>{contract.status}</StatusBadge></div><div className="grid gap-3 sm:grid-cols-3"><p><span className="block text-xs text-slate-400">Следующий платёж</span><b>{contract.premium}</b></p><p><span className="block text-xs text-slate-400">Оплатить до</span><b>{contract.nextPaymentDate}</b></p><p><span className="block text-xs text-slate-400">Защита</span><b>{contract.insuredAmount}</b></p></div><ProgressBar value={contract.paidPercent}/><div className="flex gap-3"><Button href={`/contracts/${contract.id}`}>Открыть договор</Button><Button href="/payment" variant="ghost">Оплатить</Button></div></Card>; }
