import { ChevronRight, ShieldCheck } from 'lucide-react';import { Button } from './Button';import { Card } from './Card';import { ProgressBar } from './ProgressBar';import { StatusBadge } from './StatusBadge';import type { Contract } from '@/data/mock';
export function ContractCard({ contract }: { contract: Contract }) { return <Card className="space-y-5"><div className="flex items-start justify-between gap-3"><div className="flex gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-lavender text-brand-900"><ShieldCheck /></div><div><div className="flex flex-wrap items-center gap-2"><h3 className="text-xl font-black text-brand-900">{contract.title}</h3><StatusBadge>{contract.status}</StatusBadge></div><p className="text-sm text-slate-500">{contract.type} · № {contract.number}</p></div></div><ChevronRight className="text-slate-300"/></div><div className="grid gap-3 sm:grid-cols-4"><p><span className="block text-xs text-slate-400">Взнос</span><b>{contract.premium}</b></p><p><span className="block text-xs text-slate-400">До даты</span><b>{contract.nextPaymentDate}</b></p><p><span className="block text-xs text-slate-400">Защита</span><b>{contract.insuredAmount}</b></p><p><span className="block text-xs text-slate-400">Резерв</span><b>{contract.reserve}</b></p></div><div><div className="mb-2 flex justify-between text-xs font-bold text-slate-500"><span>Накоплено</span><span>{contract.paidPercent}%</span></div><ProgressBar value={contract.paidPercent}/></div><div className="flex flex-col gap-3 sm:flex-row"><Button href={`/contracts/${contract.id}`}>Открыть договор</Button><Button href={`/contracts/${contract.id}/payment`} variant="ghost">Оплатить</Button></div></Card>; }
